# Copy to ".env" in this folder to override defaults (optional if you use application-dev.yml / default dev profile).
# Start infrastructure: docker compose up -d
# Run API: ./gradlew :khmer-code-path-api:bootRun

DB_URL=jdbc:postgresql://localhost:5433/khmercodepathdb
DB_USERNAME=postgres
DB_PASSWORD=password

REDIS_HOSTNAME=localhost
REDIS_PORT=6379

JWT_SECRET=YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY3ODkwMTIzNA==
# Access / refresh TTL: plain seconds (e.g. 3600) or Spring duration (e.g. 1h, 30m, 7d, PT15M)
TOKEN_EXPIRATION=1h
REFRESH_TOKEN_EXPIRATION=7d

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

GOOGLE_AI_API_KEY=
GOOGLE_AI_MODEL=gemini-2.0-flash
OPTION_MODEL=text-embedding-004

MINIO_URL=http://127.0.0.1:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET_NAME=khmer-code-path
